import ConfirmationClient from "./ConfirmationClient";

export const metadata = { title: "Booking confirmed — Chilly" };

export default function ConfirmationPage() {
  return <ConfirmationClient />;
}
